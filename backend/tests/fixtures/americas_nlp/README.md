All six parallel files are synthetic and development_only. They contain no
real Aymara sentences or provider data. Each .aym line matches the same line
in .es. Use synthetic=True when loading these fixtures.
